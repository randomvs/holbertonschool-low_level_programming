#include "sort.h"

/**
 *quick_sort - implement quicksort on an array of ints
 *@array: array of ints
 *@size: size of array
 */

void quick_sort(int *array, size_t size)
{
	array = array;
	size = size;
	printf("still hashing algo\n");
}
